import React from 'react';

import logo from '../../../../assets/logo.png';

import { TEXT_CONSTANTS } from '../../../../constants';

const Logo = () => <img src={logo} alt={TEXT_CONSTANTS.LOGO_ALT_TEXT} />;

export default Logo;
